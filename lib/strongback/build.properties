strongback.home=${lib}/strongback

strongback.contribs=${stronback.home}/contribs
strongback.lib=${strongback.home}/java
strongback.jar=${strongback.home}/java/strongback.jar

strongback.classpath=${classpath}:${strongback}

# Specify the version of Strongback to be downloaded and installed if missing when 'ant deps' is run
strongback.version=1.0.1

